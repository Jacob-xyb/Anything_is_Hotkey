# 快捷键



- 快捷键映射vscode

```c++
Ctrl + Enter    // 往前插入空白行，光标跟随。
Ctrl + L      // 删除当前行
Ctrl + D      // 重复当前行
ctrl + shift + backspace    // 回到上一编辑位置
ctrl + m, ctrl + m   // 折叠当前函数
Ctrl + K, Ctrl + C   // 注释
Ctrl + K, Ctrl + U   // 取消注释
shift + F12   // 查看所有引用
```